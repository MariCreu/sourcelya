services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: sourcelya
      POSTGRES_PASSWORD: sourcelya
      POSTGRES_DB: sourcelya
    ports:
      - "5432:5432"
    volumes:
      - sourcelya_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sourcelya"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+psycopg://sourcelya:sourcelya@postgres:5432/sourcelya
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    command: >
      sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"

volumes:
  sourcelya_postgres_data:
