[pytest]
testpaths = tests
markers =
    integration: requires a real PostgreSQL database (see tests/integration/README.md). Excluded by default — run with `pytest -m integration`.
addopts = -m "not integration"
