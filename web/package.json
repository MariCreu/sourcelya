{
  "name": "sourcelya-web",
  "private": true,
  "description": "sourcelya.com public site — static, zero runtime dependencies.",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && npx --yes serve . -l 5050"
  }
}
